# typescript-type-tuner

Train an encoder-decoder transformer to improve TypeScript types.

Given syntactically valid TypeScript with loose types, the model produces tighter, more specific type annotations.
