[project]
name = "core"
version = "0.1.0"
description = "Shared utilities for ai-lab"
requires-python = ">=3.12"
dependencies = [
    "qdrant-client>=1.17.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/core"]
