[project]
name = "ai-lab"
version = "0.1.0"
description = "AI experimentation monorepo"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["packages/core", "packages/ts-type-extractor", "packages/ts-type-refiner", "packages/refiner-playground"]
